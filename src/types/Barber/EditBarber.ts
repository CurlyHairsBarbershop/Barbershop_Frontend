export interface EditBarber {
  name: string;
  lastName: string;
  email: string;
  imageUrl: string;
};

export interface AddBarber {
  name: string;
  lastName: string;
  email: string;
  image: string;
};